<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/item_background"
    android:padding="10dp"
    android:layout_margin="5dp"
    android:elevation="5dp">

    <ImageView
        android:id="@+id/userImage"
        android:contentDescription="Image of object"
        android:layout_width="50dp"
        android:layout_height="50dp"
        tools:src="@tools:sample/avatars"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="6dp"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="8dp"
        />

    <TextView
        android:id="@+id/userName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="@tools:sample/full_names"
        android:textSize="20sp"
        android:textColor="@android:color/black"
        app:layout_constraintTop_toTopOf="@+id/userImage"
        app:layout_constraintStart_toEndOf="@+id/userImage"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="8dp"
        app:layout_constraintBottom_toTopOf="@+id/userExperiencePoints" />

    <TextView
        android:id="@+id/userExperiencePoints"
        android:layout_width="270dp"
        android:layout_height="20dp"
        tools:text="@tools:sample/lorem[4:10]"
        app:layout_constraintBottom_toBottomOf="@+id/userImage"
        app:layout_constraintStart_toEndOf="@+id/userImage"
        android:layout_marginStart="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginEnd="8dp"
        app:layout_constraintHorizontal_bias="0.050" />

    <ImageView
        android:id="@+id/interact"
        android:contentDescription="Icon of interaction"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_marginEnd="0dp"
        android:paddingStart="350dp"
        android:paddingEnd="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/primary_main"
        app:srcCompat="@drawable/baseline_front_hand_24" />

</androidx.constraintlayout.widget.ConstraintLayout>